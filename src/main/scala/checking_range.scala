object checking_range {

  def main(args: Array[String]): Unit = {

    val X = 20
    if (X >= 1 || X <= 10)
    {
      print("The X i in the give range")
    }
    else if (X>=20 || X<=30)
    {
      print("The x is  in the range")
    }
    else {
      print("The X is not in the range")
    }
  }
}
